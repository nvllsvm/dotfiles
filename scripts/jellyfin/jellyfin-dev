#!/usr/bin/env sh
set -e

cd ~/Code/jellyfin

OUTPUT_DIR="$(pwd)/debug"
dotnet publish \
    --configuration debug \
    --output "${OUTPUT_DIR}" \
    Jellyfin.Server

dotnet "${OUTPUT_DIR}/jellyfin.dll" -programdata ~/.jellyfin_dev
