#!/bin/sh
while true; do 
    START="$(date +%s)"
    foot --title tmux-scratchpad --app-id tmux-scratchpad -- zsh -ic 'tmux-attach'
    END="$(date +%s)"
    if [ "${START}" = "${END}" ]; then
        sleep 1
    fi
done
