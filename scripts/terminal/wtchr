#!/bin/sh

set -e

if [ -z $1 ]; then
    echo ERROR: No command provided
    exit 1
fi

while true; do
    clear
    eval "$@ &"
    trap "pkill -P $!; exit" 2
    wait
    fswatch -l 0.1 --one-event ./
done
