#!/bin/bash

STEP=5

if [[ $1 == 'mute' ]]; then
    pamixer --toggle-mute
else
    pamixer --unmute
    pamixer --$1 $STEP
fi

if [[ $(pamixer --get-mute) == "true" ]]; then
    ARGS="Mute"
else
    ARGS="-h int:value:$(pamixer --get-volume)"
fi

dunstify --replace=10002 Volume $ARGS
