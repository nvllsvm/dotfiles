[Unit]
Description=undervolt surface go
After=tlp.service
After=suspend.target

[Service]
Type=oneshot
ExecStart=/usr/bin/undervolt --core -100 --cache -100 --gpu -100

[Install]
WantedBy=multi-user.target
WantedBy=sleep.target
