[Unit]
Description=undervolt surface go
After=tlp.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStop=/usr/bin/undervolt --core -100 --cache -100 --gpu -100

[Install]
WantedBy=multi-user.target suspend.target
