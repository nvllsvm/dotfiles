#!/bin/bash
IFS=$'\n'

optimize_png ()
{
	optipng -o7 "$X"
	pngout "$X"
}

if [ "$1" = "-a" ]
then 
	for X in `find -iname '*'png`
		do
			optimize_png
		done
else
	for X in "$@"
		do
			optimize_png
		done
fi
